/**
 * Lobby player bounds for UI copy and client-side checks.
 * Keep in sync with `backend/src/config/constants.ts` (`MIN_PLAYERS_TO_START`, `LOBBY_MAX_PLAYERS`).
 */
export const MIN_LOBBY_PLAYERS_TO_START = 2;
export const MAX_LOBBY_PLAYERS = 8;
